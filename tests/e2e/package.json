{
  "name": "@chitchat/e2e",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test --list",
    "test:e2e": "playwright test",
    "lint": "echo \"No lint configured for e2e yet\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.51.0",
    "typescript": "~5.9.2"
  }
}
